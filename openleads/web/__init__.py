"""
The local-first web dashboard.

``openleads web`` boots a tiny stdlib HTTP server on localhost that serves a
single-page app and a JSON API bridging to the very same engine the CLI uses —
find, verify, write, and send, all in the browser, with your keys and data never
leaving your machine. No Node, no build step, no cloud: the UI ships pre-built as
static files in ``web/static``.
"""
